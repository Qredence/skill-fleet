# Copy this file to `.env` and fill in values you need.
#
# NOTE: The API enforces CORS:
# - In `production`, you MUST set `SKILL_FLEET_CORS_ORIGINS` (no wildcard `*`).
# - In `development`, `SKILL_FLEET_CORS_ORIGINS=*` is allowed.

# =============================================================================
# LLM Credentials (choose ONE primary path)
# =============================================================================

# Option A (direct Gemini):
# Either of these works for the default `config/config.yaml` registry.
GOOGLE_API_KEY=your_google_api_key_here
# GEMINI_API_KEY=your_gemini_api_key_here

# Option B (LiteLLM proxy):
# If you route all providers through a LiteLLM proxy, set these.
# LITELLM_API_KEY=your_litellm_key_here
# LITELLM_BASE_URL=http://localhost:4000

# =============================================================================
# Skill Fleet API (server)
# =============================================================================

# Environment: `production` (default) or `development`
# SKILL_FLEET_ENV=development

# Comma-separated list of allowed origins (required in production).
# Development-only wildcard example:
# SKILL_FLEET_CORS_ORIGINS=*
# Production example:
# SKILL_FLEET_CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Where skills live on disk (defaults to `./skills`).
# On first run, Skill Fleet bootstraps minimal `taxonomy_meta.json`, etc.
# SKILL_FLEET_SKILLS_ROOT=skills

# =============================================================================
# Skill Fleet CLI (client)
# =============================================================================

# Where the CLI points (defaults to http://localhost:8000)
# SKILL_FLEET_API_URL=http://localhost:8000

# User ID used for analytics/context (defaults to "default")
# SKILL_FLEET_USER_ID=default

# If Rich markup errors occur when printing exception messages:
# SKILL_FLEET_FORCE_PLAIN_TEXT=1

# =============================================================================
# DSPy / runtime tuning
# =============================================================================

# Optional: override DSPy cache dir (default: `.dspy_cache`)
# DSPY_CACHEDIR=.dspy_cache

# Optional: override temperature globally (note: Gemini 3 models require 1.0 for some features)
# DSPY_TEMPERATURE=1.0

# =============================================================================
# UV Package Manager
# =============================================================================

# UV cache directory:
# - Recommended: use a stable, absolute path for consistency across tools/CI.
# - NOTE: Environment variable expansion like ${HOME} is NOT reliably supported in .env files.
# - Example using home-dir shorthand (some tools expand this):
#     UV_CACHE_DIR="~/.uv_cache"
# - Alternatively, use an absolute path and replace with your real home directory:
#     UV_CACHE_DIR="/home/your-username/.uv_cache"
# - If you prefer a project-local cache (e.g., for sandboxes), you can set:
#     UV_CACHE_DIR=.uv_cache
#   and should add `.uv_cache` to your `.gitignore` to avoid committing cache files.
# UV_CACHE_DIR="~/.uv_cache"

# =============================================================================
# Model overrides (advanced)
# =============================================================================

# Override default model key (must exist in config `models.registry`)
# FLEET_MODEL_DEFAULT=gemini/gemini-3-flash-preview

# Override specific tasks or roles
# FLEET_MODEL_SKILL_UNDERSTAND=gemini/gemini-3-flash-preview
# FLEET_MODEL_SKILL_PLAN=gemini/gemini-3-pro-preview
# FLEET_MODEL_PLANNER=gemini/gemini-3-pro-preview

# =============================================================================
# Optional providers (only needed if you add these models to config registry)
# =============================================================================

# DeepInfra (example)
# DEEPINFRA_API_KEY=deepinfra-...
# DEEPINFRA_BASE_URL=https://api.deepinfra.com/v1/openai

# =============================================================================
# Database Configuration (Required)
# =============================================================================

# PostgreSQL connection string (required for skill-fleet to run)
# Example formats:
#   - Local: DATABASE_URL="postgresql://user:pass@localhost:5432/skill_fleet"
#   - Neon:  DATABASE_URL="postgresql://user:pass@ep-xxx.aws.neon.tech/neondb?sslmode=require"
#   - Supabase: DATABASE_URL="postgresql://postgres:pass@db.xxx.supabase.co:5432/postgres"
DATABASE_URL="postgresql://user:password@localhost:5432/skill_fleet?sslmode=prefer"
