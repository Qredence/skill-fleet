[{'name': 'test_discovery', 'content': 'Implements logic to scan directories for classes inheriting from `unittest.TestCase` or functions prefixed with `test_`. It supports custom configuration via `pytest.ini` or `pyproject.toml`.'}, {'name': 'test_runners', 'content': 'Provides a unified interface to trigger test runs. It handles environment variable injection, path management (PYTHONPATH), and output redirection to capture both STDOUT and STDERR.'}, {'name': 'coverage_analysis', 'content': 'Wraps the `coverage` CLI to start/stop tracking during test execution. It generates reports in JSON or XML format for programmatic analysis of missing line coverage.'}, {'name': 'failure_analyzer', 'content': 'A diagnostic utility that parses Python tracebacks. It extracts the failing line, the exception type, and the local variables at the time of failure to provide context for debugging.'}]